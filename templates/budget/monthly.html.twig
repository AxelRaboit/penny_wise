{% extends 'base.html.twig' %}

{% block title %}{{ budget.month|month_name }} {{ budget.year }} {{ parent() }}{% endblock %}

{% block body %}
    <div class="mb-4">
        {{ include('budget/partial/_main.html.twig') }}
    </div>

    <div class="mb-4">
        {{ include('budget/partial/transaction/_transactions.html.twig') }}
    </div>

    {{ include('components/note.html.twig') }}
{% endblock %}
