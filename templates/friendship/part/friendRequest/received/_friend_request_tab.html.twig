<button data-tab="requests" class="tab-button py-2 px-4 w-full md:w-auto relative border-b-2 border-transparent text-dynamic">
    Pending Requests
    {% if pendingRequestsCount > 0 %}
        <span class="absolute top-0 right-0 transform translate-x-2 -translate-y-2 bg-accent-primary text-dynamic text-xs font-bold rounded-full px-2 py-0.5">
            {{ pendingRequestsCount }}
        </span>
    {% endif %}
</button>
