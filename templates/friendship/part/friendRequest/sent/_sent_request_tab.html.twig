<button data-tab="sent_requests" class="tab-button py-2 px-4 w-full md:w-auto relative border-b-2 border-transparent text-white">
    Sent Requests
    {% if sentRequestsCount > 0 %}
        <span class="absolute top-0 right-0 transform translate-x-2 -translate-y-2 bg-accent-primary text-white text-xs font-bold rounded-full px-2 py-0.5">
            {{ sentRequestsCount }}
        </span>
    {% endif %}
</button>
