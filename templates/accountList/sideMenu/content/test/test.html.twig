{% set testModalContent %}
    Ceci est du contenu test
{% endset %}

{{ include('components/rightSideMenu/rightSideMenu.html.twig', {
    modalId: 'accountListSideMenuTestModal',
    modalTitle: 'Test',
    modalContent: testModalContent,
    closeModalId: 'closeAccountListSideMenuTestModal'
}) }}
