{% extends 'base.html.twig' %}

{% block title %}Share{% endblock %}

{% block body %}
    <h1>Share</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ share.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ share.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ share.createdAt ? share.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ share.updatedAt ? share.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_share_index') }}">back to list</a>

    <a href="{{ path('app_share_edit', {'id': share.id}) }}">edit</a>

    {{ include('share/_delete_form.html.twig') }}
{% endblock %}
