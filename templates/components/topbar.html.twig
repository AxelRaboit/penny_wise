<div class="bg-primary px-5 py-3">
    <div class="flex items-center justify-between">
        <div class="text-white font-bold text-xl">
            SPENDING WISE
        </div>

        {% if app.user %}
            <div class="block md:hidden">
                <button id="js-menu-toggle" class="text-white focus:outline-none">
                    <i class="fa-solid fa-bars text-xl"></i>
                </button>
            </div>

            <div class="hidden md:flex items-center space-x-4" id="menu">
                <a href="{{ path('app_homepage') }}" class="text-center font-bold text-white p-2 cursor-pointer rounded">
                    <i class="fa-solid fa-house mr-1"></i>
                    Home
                </a>
                {# <a href="{{ path('budget_new') }}" class="text-center font-bold text-white p-2 cursor-pointer rounded">
                    <i class="fa-solid fa-plus mr-1"></i>
                    Budget
                </a>
                <a href="{{ path('transaction_new') }}" class="text-center font-bold text-white p-2 cursor-pointer rounded">
                    <i class="fa-solid fa-plus mr-1"></i>
                    Transaction
                </a> #}
                <a href="#" class="text-center font-bold text-white p-2 cursor-pointer rounded">
                    <i class="fa-solid fa-screwdriver-wrench mr-1"></i>
                    Admin panel
                </a>
                <a href="#" class="text-center font-bold text-white p-2 cursor-pointer rounded">
                    <i class="fa-solid fa-user mr-1"></i>
                    {{ app.user.email }}
                </a>
                <a href="{{ path('app_logout') }}" class="text-center font-bold text-white p-2 cursor-pointer rounded">
                    <i class="fa-solid fa-right-from-bracket mr-1"></i>
                </a>
            </div>
        {% endif %}
    </div>

    {{ include('partial/_burger_menu.html.twig') }}
</div>

{% block javascripts %}
    {{ importmap('topbar') }}
{% endblock %}
