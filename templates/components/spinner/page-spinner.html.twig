<div id="{{ spinnerId }}" class="fixed inset-0 flex items-center justify-center z-50">
    <div class="spinner animate-spin rounded-full border-4 border-t-4 border-white border-t-transparent w-10 h-10"></div>
</div>
