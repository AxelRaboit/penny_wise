<div id="{{ spinnerId }}" class="fixed inset-0 flex items-center justify-center bg-gray-800 bg-opacity-75 z-50">
    <div class="spinner animate-spin rounded-full border-4 border-t-4 border-white border-t-transparent w-24 h-24"></div>
</div>
