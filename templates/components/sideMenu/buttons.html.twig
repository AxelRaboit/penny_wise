{% for button in buttons %}
    <button id="{{ button.id }}" class="bg-secondary hover:bg-secondary-hover text-white py-1 px-2 rounded-md mb-2" title="{{ button.title }}">
        <i class="fa-solid {{ button.icon }}"></i>
    </button>
{% endfor %}
