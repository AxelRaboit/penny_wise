<div id="graphsBubble" x-show="hoverAll" x-transition.opacity.duration.300ms
     class="fixed bottom-20 right-4 bg-secondary hover:bg-secondary-hover text-white py-4 px-5 rounded-full cursor-pointer shadow-lg z-40">
    <i class="fa-solid fa-chart-simple"></i>
</div>

<div id="graphsModal" class="fixed top-0 right-0 w-full md:w-1/3 h-full bg-accent shadow-lg transform translate-x-full transition-transform duration-300 ease-in-out z-50 overflow-hidden">
    <div class="bg-secondary text-white p-4 flex justify-between items-center">
        <div class="flex items-center justify-center">
            <h2 class="text-md font-bold text-white mr-5">Graphs</h2>
        </div>
        <button id="closeGraphsModal" class="text-white hover:text-gray-200">
            <i class="fa-solid fa-times text-xl"></i>
        </button>
    </div>

    <div class="p-4 h-[calc(100%-4rem)] overflow-y-auto">
        {{ include('account/wallet/partial/_chart_card.html.twig', {
            title: 'Left to spend',
            chartId: 'chart-left-to-spend',
            chart: leftToSpendChart.data.isDataPresent ? leftToSpendChart : null
        }) }}
        {{ include('account/wallet/partial/_chart_card.html.twig', {
            title: 'Total monthly spent',
            buttons: [
                {chartType: 'monthly', chartId: 'chart-monthly-spent', chartFormat: 'bar', label: 'Bar'},
                {chartType: 'monthly', chartId: 'chart-monthly-spent', chartFormat: 'line', label: 'Line'},
            ],
            chartId: 'chart-monthly-spent',
            chart: totalSpendingForCurrentAndPreviousNthMonthsChart
        }) }}

        {{ include('account/wallet/partial/_chart_card.html.twig', {
            title: 'Total yearly spent',
            buttons: [
                {chartType: 'yearly', chartId: 'chart-yearly-spent', chartFormat: 'bar', label: 'Bar'},
                {chartType: 'yearly', chartId: 'chart-yearly-spent', chartFormat: 'line', label: 'Line'},
            ],
            chartId: 'chart-yearly-spent',
            chart: totalSpendingYearsChart
        }) }}
    </div>
</div>
