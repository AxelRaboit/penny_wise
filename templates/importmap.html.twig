{% set importmapEntries = ['app', 'topbar', 'side_menu_modal'] %}

{# Pages #}

{# Account #}
{# Account - Wallet - Dashboard #}
{% if page_requires_account_wallet_dashboard is defined and page_requires_account_wallet_dashboard is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['account_wallet_dashboard']) %}
{% endif %}

{# Account - Wallet - Dashboard - Transaction #}
{% if page_account_wallet_dashboard_transaction_list is defined and page_account_wallet_dashboard_transaction_list is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['account_wallet_dashboard_transaction_list']) %}
{% endif %}

{% if page_requires_account_wallet_dashboard_transaction_new is defined and page_requires_account_wallet_dashboard_transaction_new is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['account_wallet_dashboard_transaction_new']) %}
{% endif %}

{% if page_requires_account_wallet_dashboard_transaction_edit is defined and page_requires_account_wallet_dashboard_transaction_edit is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['account_wallet_dashboard_transaction_edit']) %}
{% endif %}

{% if page_requires_account_wallet_dashboard_transaction_show is defined and page_requires_account_wallet_dashboard_transaction_show is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['account_wallet_dashboard_transaction_show']) %}
{% endif %}

{# Account List #}
{% if page_requires_account_list is defined and page_requires_account_list is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['account_list']) %}
{% endif %}

{# Transaction #}
{% if page_requires_transaction_tag_list is defined and page_requires_transaction_tag_list is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['transaction_tag_list']) %}
{% endif %}

{% if page_requires_transaction_tag_crud is defined and page_requires_transaction_tag_crud is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['transaction_tag_crud']) %}
{% endif %}

{# Partials #}
{% if page_requires_transaction_fields is defined and page_requires_transaction_fields is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['transaction_fields']) %}
{% endif %}

{# Components #}
{# CODE HERE #}

{{ importmap(importmapEntries) }}
