{% set importmapEntries = ['app', 'topbar'] %}

{# Pages #}
{% if page_requires_monthly is defined and page_requires_monthly is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['monthly']) %}
{% endif %}

{% if page_requires_account_list is defined and page_requires_account_list is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['account_list']) %}
{% endif %}

{% if page_requires_transaction_tag_list is defined and page_requires_transaction_tag_list is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['transaction_tag_list']) %}
{% endif %}

{% if page_requires_transaction_tag_crud is defined and page_requires_transaction_tag_crud is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['transaction_tag_crud']) %}
{% endif %}

{# Partials #}
{% if page_requires_transaction is defined and page_requires_transaction is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['transaction']) %}
{% endif %}

{% if page_requires_transaction_fields is defined and page_requires_transaction_fields is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['transaction_fields']) %}
{% endif %}

{# Components #}
{% if page_requires_note is defined and page_requires_note is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['note']) %}
{% endif %}

{% if page_requires_graphs is defined and page_requires_graphs is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['graphs']) %}
{% endif %}

{% if page_requires_actions is defined and page_requires_actions is same as(true) %}
    {% set importmapEntries = importmapEntries|merge(['actions']) %}
{% endif %}

{{ importmap(importmapEntries) }}
